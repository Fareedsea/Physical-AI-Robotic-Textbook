# Task 2: Clean Default Template & Install Dependencies

## Summary
Cleaned up the default Docusaurus template files and installed necessary dependencies including `react-markdown` and `react-icons`.

## Task Number
2

## Files Changed
- `frontend/docs/*` (Deleted)
- `frontend/blog/*` (Deleted)
- `frontend/src/components/HomepageFeatures` (Deleted)
- `frontend/src/pages/index.tsx` (Modified)
- `frontend/package.json` (Modified)
- `frontend/package-lock.json` (Modified)

## Why Change Was Needed
To prepare the project for the custom textbook content and design, removing the default tutorial content.

## Git Commit Message
```bash
git add .
git commit -m "chore: clean default template and install dependencies (Task 2)" -m "Removed default docs, blog posts, and components. Installed react-markdown, remark-gfm, and react-icons."
```
